# Saeed AI — Windows desktop migration

The new Windows foundation is a real C#/.NET 8 WPF application. It owns the desktop window, transparency, always-on-top behavior and monitor/work-area placement. WebView2 is only the 3D rendering surface; the host is C#.

Next: load the real saeed.glb, add animation, port the agent/tool layer to C#, then package Saeed.exe and an installer.